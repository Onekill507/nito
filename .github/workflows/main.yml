name: AtlasOS Free Windows 11 (6h)

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: windows-latest
    steps:
      - name: Show AtlasOS v0.5.0 Prerequisites
        run: |
          echo "Windows 11 Pro/Enterprise/Education 24H2/25H2 x64"
          echo "Clean install, Admin rights, Internet required"
      - name: Free 6-hour window
        run: echo "Runner active for up to 6 hours (GitHub free tier)"
